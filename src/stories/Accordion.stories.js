import React from 'react';
import { Accordion } from '@mui/material';

export default {
  title: 'MUI/Accordion',
  component: Accordion,
};

export const Default = () => <Accordion>Accordion</Accordion>;